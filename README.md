# docx-to-markdown Project

This project provides a GitHub Actions workflow to automatically convert `.docx` files in the
repository to Markdown format using `pandoc`. The generated Markdown files are committed back to
the repository to enhance readability of diffs.
